public class IceMasterEffectMod : IEffectModifier
{
  public void Process(EffectExecutionContext ctx)
  {
    // if (ctx.Effect is not EffSpawnIce ice)
    //   return;

    // missile.IncreaseCount(1); // (+1 missile globally)
  }
}
